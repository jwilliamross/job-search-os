# Resources

- `linkedin-export.md`: how to export your connections for the outreach skill.
- Vendored: blader/humanizer (MIT) at `.claude/skills/humanizer/`; Aboudjem/humanizer-skill
  CLI (MIT) at `scripts/humanizer-metrics/`; humanizer-tools/slop-humanizer (MIT) checklist
  under `.claude/skills/humanizer/references/`.
- Job scraping: python-jobspy (`pip install python-jobspy`), used by `scripts/search_jobs.py`.
- PDF rendering: headless Chromium (Playwright's browser install works;
  `scripts/render_letter.py` finds it under `/opt/pw-browsers` or on PATH).
